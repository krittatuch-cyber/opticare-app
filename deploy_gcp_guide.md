# คู่มือการเอาแอปขึ้น Google Cloud (Deploy to Google Cloud Run 🚀)

เพื่อเปิดให้บุคคลภายนอกเข้ามาร่วมทดสอบระบบจัดการร้านแว่นตา **Eye Focus** ได้จากทุกที่ วิธีที่ง่าย ปลอดภัย และมีค่าใช้จ่ายเริ่มต้นเป็น **ฟรี 0 บาท (Free Tier)** คือการขึ้นระบบบน **Google Cloud Run** ซึ่งเป็นบริการ Serverless Containers ของทาง Google Cloud

เนื่องจากเครื่องคอมพิวเตอร์ของคุณยังไม่มีโปรแกรม `gcloud` วิธีการที่ง่ายที่สุดและไม่ต้องลงโปรแกรมใดๆ คือการใช้งาน **Google Cloud Shell** ในเว็บเบราว์เซอร์ของคุณ โดยอิงไฟล์จาก `opticare-app.zip` ที่มีอยู่โฟลเดอร์นี้ครับ:

---

## 🛠️ อุปกรณ์ที่เตรียมไว้ให้แล้วในโปรเจกต์
1. `Dockerfile` - ไฟล์ระบุการจำลองระบบปฏิบัติการ, ติดตั้ง Python, ย้ายโค้ดเว็บ และสั่งรันเซิร์ฟเวอร์
2. `requirements.txt` - รายการโปรแกรม Python ที่ต้องใช้รันหลังบ้าน (FastAPI, Uvicorn)
3. `opticare-app.zip` - ไฟล์รวมโค้ดเว็บและหลังบ้านทั้งหมด

---

## 🚀 ขั้นตอนการขึ้นระบบ (Step-by-Step Deployment)

### ขั้นตอนที่ 1: เข้าสู่ Google Cloud Shell
1. เปิดเว็บเบราว์เซอร์ไปที่ลิงก์ **[console.cloud.google.com](https://console.cloud.google.com/)**
2. ล็อกอินด้วยบัญชี Google/Gmail ของคุณ
3. สังเกตที่ปุ่มขวาบนของหน้าเว็บ จะมีไอคอนรูปกล่องควบคุม **Activate Cloud Shell** `>_` ให้กดคลิกเพื่อเปิดเครื่องคอมพิวเตอร์จำลองฟรีของ Google ขึ้นมา (จะปรากฏหน้าจอเทอร์มินัลสีดำด้านล่าง)

---

### ขั้นตอนที่ 2: อัปโหลดไฟล์ Zip ขึ้นระบบ
1. ในหน้าต่างสีดำ (Cloud Shell) ด้านล่าง ให้กดปุ่ม **จุดสามจุด (More)** ที่มุมขวาบนของหน้าต่าง Cloud Shell -> เลือก **Upload**
2. เลือกอัปโหลดไฟล์ **`opticare-app.zip`** จากโฟลเดอร์โปรเจกต์ของคุณขึ้นไป

---

### ขั้นตอนที่ 3: แตกไฟล์และเริ่มดีพลอยด์
ในหน้าต่างเทอร์มินัล Cloud Shell ให้คัดลอกคำสั่งด้านล่างนี้ไปวางแล้วกด Enter เพื่อแตกไฟล์และสั่งดีพลอยด์ทันที:

```bash
# 1. แตกไฟล์ซิปไปที่โฟลเดอร์ opticare-app
unzip opticare-app.zip -d opticare-app

# 2. ย้ายเข้าไปในโฟลเดอร์ของแอป
cd opticare-app

# 3. สั่งดีพลอยด์เว็บขึ้น Google Cloud Run ทันที
gcloud run deploy opticare-app \
  --source . \
  --region asia-east1 \
  --allow-unauthenticated
```

---

## 📝 สิ่งที่จะเกิดขึ้นระหว่างรันคำสั่ง (Interactive Prompts)
ระหว่างที่ดีพลอยด์ ระบบของ Google จะถามคุณ 1-2 คำถาม ให้ตอบดังนี้:
1. **Enable APIs**: หากระบบถามว่าต้องการให้เปิดใช้งาน Artifact Registry และ Cloud Run API หรือไม่?
   * ให้พิมพ์ `y` แล้วกด Enter
2. **Create Repository**: หากระบบถามเพื่อขอสร้าง Repository เพื่อเก็บรูปตู้จำลอง
   * ให้กด Enter เพื่อยอมรับค่าเริ่มต้น

---

## 🎯 ตรวจสอบผลลัพธ์การขึ้นระบบ
เมื่อระบบทำงานเสสิ้น (ใช้เวลาประมาณ 2-3 นาที) หน้าจอเทอร์มินัลจะแสดงลิงก์ภายนอกส่งท้ายตัวอย่างเช่น:

```text
Service [opticare-app] revision [opticare-app-00001-xxx] has been deployed and is serving 100% of traffic.
Service URL: https://opticare-app-xxxxxx-de.a.run.app
```

คุณสามารถคัดลอก **Service URL** ที่ปรากฏไปส่งให้เพื่อน ทีมงาน หรือลูกค้า เพื่อเข้าใช้บริการและร่วมทดสอบความถูกต้องของระบบร้านแว่นตาระดับลักชัวรีนี้จากภายนอกได้ทันที 24 ชั่วโมงครับ!
