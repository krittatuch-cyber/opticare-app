# คู่มือการเอาแอปขึ้น Cloudflare Pages (Deploy via Direct Upload ☁️)

การนำระบบร้านแว่นตา **Eye Focus** ขึ้นคลาวด์ของ **Cloudflare** มีข้อดีคือความเร็วสูง เสถียรภาพ และ **ฟรี 100%** (Pages + D1 Database) 

เนื่องจากเครื่องคอมพิวเตอร์ของคุณไม่มี Git หรือ Node.js การติดตั้งผ่านวิธี **Direct Upload (อัปโหลดไฟล์ซิปโดยตรง)** บนหน้าเว็บของ Cloudflare จึงเป็นวิธีที่ง่ายที่สุด สะดวกที่สุด และไม่ต้องเขียนโค้ดสั่งดีพลอยด์ใดๆ ครับ โดยใช้งานไฟล์ `opticare-app.zip` ที่มีอยู่ในโฟลเดอร์นี้ครับ

---

## 🚀 ขั้นตอนการขึ้นระบบ Cloudflare (แบบไม่อิง Git)

### ขั้นตอนที่ 1: ตั้งค่าฐานข้อมูล Cloudflare D1 (Database)
1. เข้าเว็บไซต์ **[dash.cloudflare.com](https://dash.cloudflare.com/)** และล็อกอินเข้าระบบ
2. ที่เมนูด้านซ้าย เลือก **Workers & Pages** -> เลือกแท็บย่อย **D1**
3. กดปุ่ม **Create database** -> เลือกแท็บ **Dashboard**
4. ตั้งชื่อฐานข้อมูลว่า `opticare-db` แล้วกดปุ่ม **Create**
5. เมื่อสร้างเสร็จแล้ว ให้กดคลิกเข้าไปที่ชื่อฐานข้อมูล `opticare-db` -> ไปที่แท็บ **Console**
6. คัดลอกคำสั่ง SQL ทั้งหมดในไฟล์ `schema.sql` (อยู่ในโฟลเดอร์โปรเจกต์นี้) ไปวางในกล่อง Console ของหน้าเว็บ Cloudflare แล้วกดปุ่ม **Execute**
   * *ระบบคลาวด์จะทำการสร้างตารางข้อมูลและใส่ข้อมูลเริ่มต้น (Seed) ให้ทำงานร่วมกันทันที*

---

### ขั้นตอนที่ 2: สร้างและอัปโหลดแอป Pages (Direct Upload)
1. ที่เมนูด้านซ้าย เลือก **Workers & Pages** -> เลือกแท็บย่อย **Overview**
2. กดปุ่ม **Create** ด้านขวาบน -> เลือกแท็บย่อย **Pages**
3. เลือกหัวข้อ **Upload assets** (การอัปโหลดไฟล์ตรงโดยไม่ผ่าน Git)
4. ตั้งชื่อโปรเจกต์ของคุณ (เช่น `opticare-app`) -> กด **Create project**
5. ในหน้าอัปโหลด ให้เลือกอัปโหลดไฟล์ซิป **`opticare-app.zip`** ที่อยู่ในโฟลเดอร์โปรเจกต์นี้
6. เมื่ออัปโหลดและประมวลผลเสร็จแล้ว ให้กดปุ่ม **Deploy site**
   * *Cloudflare จะให้ลิงก์หน้าเว็บชั่วคราวแก่คุณทันที แต่ตอนนี้น่าจะยังล็อกอินเข้าไม่ได้เพราะยังไม่ได้เชื่อมฐานข้อมูล D1*

---

### ขั้นตอนที่ 3: เชื่อมฐานข้อมูล D1 เข้ากับแอป Pages
1. เมื่อกลับมาที่หน้ารายละเอียดโปรเจกต์ Pages (เช่น `opticare-app`) ให้คลิกแท็บ **Settings** ด้านบน
2. ที่เมนูด้านซ้าย เลือกหัวข้อ **Functions**
3. เลื่อนหน้าจอลงมาด้านล่างสุดที่หัวข้อ **D1 database bindings** -> กดปุ่ม **Add binding**
4. กรอกข้อมูลการเชื่อมต่อดังนี้:
   * **Variable name**: ต้องพิมพ์คำว่า `DB` (ตัวอักษรพิมพ์ใหญ่ทั้งหมด)
   * **D1 database**: เลือกฐานข้อมูลชื่อ `opticare-db` ที่สร้างไว้ในขั้นตอนที่ 1
5. กดปุ่ม **Save**
6. **สำคัญมาก**: ไปที่แท็บ **Deployments** ด้านบน -> เลือกการดีพลอยด์รายการล่าสุด -> กดปุ่มจุดสามจุดด้านขวา -> เลือก **Retry deployment** (เพื่อรันการอัปโหลดซ้ำให้ระบบผูกฐานข้อมูลทำงานร่วมกับโค้ดหลังบ้าน)

---

## 🎯 เสร็จสิ้นการดีพลอยด์
ระบบจะประมวลผลประมาณ 1 นาที เมื่อเสร็จเรียบร้อยแล้ว ลิงก์หน้าเว็บ (เช่น `https://opticare-app.pages.dev`) จะเปิดให้บริการ และสามารถเข้าสู่ระบบเพื่อร่วมทดสอบใช้งานร่วมกับระบบฐานข้อมูลคลาวด์ได้จริงจากทุกหนทุกแห่งทั่วโลกตลอด 24 ชั่วโมงทันทีครับ!
